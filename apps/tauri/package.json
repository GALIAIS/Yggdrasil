{
  "name": "@yggdrasil/tauri",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "tauri dev",
    "dev:verbose": "tauri dev --verbose",
    "dev:no-watch": "tauri dev --no-watch",
    "build": "tauri build --debug --no-bundle",
    "build:release": "tauri build --no-bundle",
    "package": "tauri build --bundles nsis --ci --no-sign",
    "package:msi": "tauri build --bundles msi --ci --no-sign"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.1.1"
  }
}
